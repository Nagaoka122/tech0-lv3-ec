# 定番商品リピート購入支援機能 設計仕様書

- 版：0.1（実装に向けた設計初稿）
- 作成日：2026年9月9日
- 関連文書：[要求定義書](./要求定義書.md)、[要件定義書](./要件定義書.md)
- 本書の技術構成・未確認の細部は設計提案である。本書作成時点でアプリの実装は行っていない。
- 本書はGUを題材にした学習用アプリの設計であり、GU公式サービスの仕様を示すものではない。

---

## 1. システム構成

### 1.1 全体構成

```mermaid
flowchart LR
    U["購入者ブラウザ"] -->|HTTPS 同一オリジン| BFF["BFF<br/>Next.js Route Handler"]
    BFF -->|内部ネットワーク| API["Backend API<br/>FastAPI"]
    POS["店頭POS<br/>Lv2改修版"] -->|専用資格情報| API
    BATCH["日次バッチ"] --> SVC
    API --> SVC["業務サービス層<br/>販売状態判定・在庫更新"]
    SVC --> DB[("PostgreSQL")]
```

購入者のブラウザは BFF とのみ通信し、Backend API を直接呼ばない。BFF は同一オリジンで
Cookie を受け取り、内部ネットワーク経由で API を呼び出す。API はインターネットに直接公開
しない。

在庫更新と販売状態判定のロジックは業務サービス層に集約し、API・バッチ・POS連携から共通で
利用する。データベースの数値を管理画面から直接書き換える運用は行わない。

### 1.2 技術構成

| 層 | 採用 | 選定理由 |
|---|---|---|
| Frontend | Next.js（TypeScript） | 型定義により、APIの入出力をコンパイル時に検査する |
| BFF | Next.js Route Handler | ブラウザと同一オリジンで Cookie を扱い、API を非公開に保つ |
| Backend | FastAPI（Python） | Pydantic による入力検証。Lv2のPOSと同じ言語で扱える |
| ORM | SQLAlchemy + Alembic | 問い合わせのパラメータバインドとスキーマ移行 |
| DB | PostgreSQL | 在庫スナップショットの蓄積と、行ロックによる同時実行制御 |
| バッチ | Python スクリプト（cron 等で起動） | 業務サービス層を共有する |

採用バージョンは実装開始時にサポート状況と既知の脆弱性を確認して固定する（8.8）。

---

## 2. UML

### 2.1 ユースケース図

```mermaid
flowchart LR
    C(("購入者"))
    P(("店頭POS"))
    B(("日次バッチ"))

    subgraph SYS["定番商品リピート購入支援機能"]
        direction TB
        UC1["UC-01<br/>販売状態を確認する"]
        UC2["UC-02<br/>同一SKUで再購入する"]
        UC3["UC-03<br/>再入荷通知に登録する"]
        UC4["UC-04<br/>販売を在庫へ反映する"]
        UC5["UC-05<br/>在庫を記録し再判定する"]
    end

    C --> UC1
    C --> UC2
    C --> UC3
    P --> UC4
    B --> UC5
    UC2 -.->|include| UC1
    UC5 -.->|affects| UC1
```

### 2.2 アクティビティ図：日次バッチによる販売状態判定

本機能の中核となる処理である。「品切れ」と「終売」を分ける判断がここで行われる。

```mermaid
flowchart TD
    START([日次バッチ開始]) --> SNAP["全SKUの在庫合計を当日分として記録<br/>sku_id と snapshot_date が重複する場合は記録しない"]
    SNAP --> LOOP{"未評価のSKUがあるか"}
    LOOP -->|なし| FIN([終了])
    LOOP -->|あり| VALID{"SKUまたは品番が<br/>無効化されているか"}
    VALID -->|はい| DISC1["販売状態 = 終売<br/>理由 = 販売終了"]
    VALID -->|いいえ| ENOUGH{"スナップショットが<br/>30日分そろっているか"}
    ENOUGH -->|いいえ| UNKNOWN["販売状態 = 判定不可<br/>理由 = 在庫記録が不足"]
    ENOUGH -->|はい| CALC["連続在庫0日数を再計算<br/>最後に在庫があった日からの経過日数"]
    CALC --> ZERO{"連続在庫0日数<br/>30日以上か"}
    ZERO -->|はい| DISC2["販売状態 = 終売<br/>理由 = 30日以上入荷なし"]
    ZERO -->|いいえ| PEND["販売状態 = 判定保留<br/>表示時に現在庫を見て<br/>販売中か品切れかを決める"]
    DISC1 --> SAVE["連続在庫0日数と判定日時を保存"]
    DISC2 --> SAVE
    UNKNOWN --> SAVE
    PEND --> SAVE
    SAVE --> LOOP
```

**連続在庫0日数を「加算」ではなく「再計算」にしている理由**：カウンタを加算する方式では、
バッチが同日に二重実行されたときに日数が過大になる。スナップショットから毎回導出すれば、
何回実行しても結果が同じになる（冪等）。障害からの再実行を安全にするための設計である。

導出の考え方（例示）：

```sql
-- 最後に在庫があった日を求め、そこからの経過日数を連続在庫0日数とする
WITH last_in_stock AS (
    SELECT sku_id, MAX(snapshot_date) AS last_date
    FROM inventory_snapshot
    WHERE total_on_hand > 0
    GROUP BY sku_id
)
SELECT s.id AS sku_id,
       CURRENT_DATE - COALESCE(l.last_date, :records_start_date) AS out_of_stock_days
FROM sku s
LEFT JOIN last_in_stock l ON l.sku_id = s.id;
```

### 2.3 シーケンス図

#### SD-01 購入履歴の表示（UC-01）

```mermaid
sequenceDiagram
    participant B as ブラウザ
    participant F as BFF
    participant A as API
    participant D as PostgreSQL

    B->>F: GET /api/me/orders (Cookie に access_token)
    F->>F: Cookie からトークンを取り出す
    F->>A: GET /me/orders (Authorization ヘッダ)
    A->>A: トークン検証 → member_id を取得
    Note over A: URLに会員IDを含めない。<br/>トークンの sub のみを会員の特定に使う
    A->>D: 注文明細を member_id と過去3年で絞り込み
    D-->>A: 履歴行
    A->>D: 対象SKUの現在庫と販売状態を取得
    D-->>A: 在庫・判定結果
    A->>A: 判定保留のSKUを現在庫で販売中／品切れに確定
    A-->>F: 履歴と販売状態
    F-->>B: JSON（内部IDは含めない）
```

#### SD-02 履歴からの再購入（UC-02）

```mermaid
sequenceDiagram
    participant B as ブラウザ
    participant F as BFF
    participant A as API
    participant D as PostgreSQL

    B->>F: POST /api/me/orders {sku_id, quantity, expected_unit_price_yen}
    F->>A: POST /me/orders
    A->>A: トークン検証、入力値の型と範囲を検証
    A->>D: BEGIN
    A->>D: SELECT ... FOR UPDATE で対象SKUの在庫行をロック
    D-->>A: 現在庫
    alt 在庫が不足
        A->>D: ROLLBACK
        A-->>F: 409 在庫がありません
    else 在庫あり
        A->>A: 単価をDBから取得し金額を再計算
        alt 再計算額が expected_unit_price_yen と不一致
            A->>D: ROLLBACK
            A-->>F: 409 価格が変更されています
        else 一致
            A->>D: 在庫を減算し、注文と明細を作成
            A->>D: COMMIT
            A-->>F: 201 {order_id}
        end
    end
    F-->>B: 結果
```

金額の照合を行う理由は改ざん対策だけではない。本機能は**去年の履歴から購入する**ため、
価格改定が起きている可能性が通常の購入より高い。サーバ側で再計算し、購入者が見ていた価格と
異なる場合は確認を求める（F-08）。

#### SD-03 店頭POS販売の在庫反映（UC-04）

```mermaid
sequenceDiagram
    participant P as 店頭POS
    participant A as API
    participant D as PostgreSQL

    P->>A: POST /pos/sales {event_id, store_id, sku_id, quantity, occurred_at}
    A->>A: POS専用資格情報を検証
    A->>A: occurred_at が許容範囲内かを検証
    A->>D: BEGIN
    A->>D: INSERT INTO pos_sale_event ... ON CONFLICT (client_id, event_id) DO NOTHING
    alt 挿入件数が0（処理済み）
        A->>D: COMMIT
        A-->>P: 200 受理済み（在庫は変更しない）
    else 新規イベント
        A->>D: SELECT ... FOR UPDATE で店舗×SKUの在庫行をロック
        A->>D: 在庫を減算（0未満にしない）
        A->>D: COMMIT
        A-->>P: 202 受理
    end
```

### 2.4 クラス図

```mermaid
classDiagram
    class Member {
        +UUID id
        +string email
        +string passwordHash
    }
    class Product {
        +UUID id
        +string productCode
        +string name
        +bool active
    }
    class Sku {
        +UUID id
        +string color
        +string size
        +string barcode
        +int priceYen
        +bool active
        +isSellable() bool
    }
    class Inventory {
        +int onHand
        +datetime updatedAt
        +decrease(int qty) void
    }
    class InventorySnapshot {
        +date snapshotDate
        +int totalOnHand
    }
    class SalesStatus {
        +StatusCode status
        +int outOfStockDays
        +string reason
        +datetime evaluatedAt
    }
    class Order {
        +UUID id
        +Channel channel
        +datetime orderedAt
        +int totalYen
    }
    class OrderLine {
        +int quantity
        +int purchasedUnitPriceYen
    }
    class RestockSubscription {
        +SubscriptionState state
        +datetime notifiedAt
    }
    class SalesStatusEvaluator {
        +evaluateAll() void
        +countOutOfStockDays(Sku) int
        +resolveForDisplay(Sku) StatusCode
    }

    Member "1" --> "*" Order
    Order "1" --> "*" OrderLine
    OrderLine "*" --> "1" Sku
    Product "1" --> "*" Sku
    Sku "1" --> "*" Inventory
    Sku "1" --> "*" InventorySnapshot
    Sku "1" --> "1" SalesStatus
    Member "1" --> "*" RestockSubscription
    RestockSubscription "*" --> "1" Sku
    SalesStatusEvaluator ..> InventorySnapshot : 参照
    SalesStatusEvaluator ..> SalesStatus : 更新
```

`SalesStatusEvaluator` は日次バッチと表示時の両方から使われる。`evaluateAll()` がバッチ、
`resolveForDisplay()` が表示時のリアルタイム確定を担う。

---

## 3. ER図

```mermaid
erDiagram
    MEMBER ||--o{ ORDERS : places
    ORDERS ||--|{ ORDER_LINE : contains
    PRODUCT ||--|{ SKU : has
    SKU ||--o{ ORDER_LINE : "referenced by"
    SKU ||--o{ INVENTORY : "stocked as"
    STORE ||--o{ INVENTORY : holds
    SKU ||--o{ INVENTORY_SNAPSHOT : "recorded as"
    SKU ||--|| SALES_STATUS : "evaluated as"
    MEMBER ||--o{ RESTOCK_SUBSCRIPTION : registers
    SKU ||--o{ RESTOCK_SUBSCRIPTION : targets
    STORE ||--o{ POS_SALE_EVENT : reports
    SKU ||--o{ POS_SALE_EVENT : "sold as"

    MEMBER {
        uuid id PK
        string email UK
        string password_hash
        timestamp created_at
    }
    PRODUCT {
        uuid id PK
        string product_code UK
        string name
        string category
        boolean active
    }
    SKU {
        uuid id PK
        uuid product_id FK
        string color
        string size
        string barcode UK
        integer price_yen
        boolean active
    }
    STORE {
        uuid id PK
        string name
        boolean active
    }
    INVENTORY {
        uuid id PK
        uuid store_id FK
        uuid sku_id FK
        integer on_hand
        timestamp updated_at
    }
    INVENTORY_SNAPSHOT {
        uuid id PK
        uuid sku_id FK
        date snapshot_date
        integer total_on_hand
    }
    SALES_STATUS {
        uuid sku_id PK
        string status
        integer out_of_stock_days
        string reason
        timestamp evaluated_at
    }
    ORDERS {
        uuid id PK
        uuid member_id FK
        uuid store_id FK
        string channel
        integer total_yen
        timestamp ordered_at
    }
    ORDER_LINE {
        uuid id PK
        uuid order_id FK
        uuid sku_id FK
        integer quantity
        integer purchased_unit_price_yen
    }
    RESTOCK_SUBSCRIPTION {
        uuid id PK
        uuid member_id FK
        uuid sku_id FK
        string state
        timestamp created_at
        timestamp notified_at
    }
    POS_SALE_EVENT {
        uuid id PK
        string client_id
        string event_id
        uuid store_id FK
        uuid sku_id FK
        integer quantity
        timestamp occurred_at
        timestamp processed_at
    }
```

### 3.1 設計上の要点

| 項目 | 設計 | 理由 |
|---|---|---|
| `ORDER_LINE.sku_id` | 品番ではなくSKUを参照する | 色・サイズを記録しなければ「同じものを買う」が成立しない。後からSKU化しても過去の取引の色・サイズは復元できない |
| `INVENTORY_SNAPSHOT` | SKU単位の全社合計を日次で保持 | 連続在庫0日数の唯一の根拠。記録開始前の期間は復元できない |
| スナップショットの粒度 | 店舗別ではなくSKU単位の合計 | 本機能の判定は「買えるかどうか」であり、どの店舗にあるかを問わない。店舗別にするとデータ量が店舗数倍になる |
| `PRODUCT.active` / `SKU.active` | 物理削除せず無効フラグで表す | 削除すると購入履歴から商品名・画像を引けなくなる |
| `SALES_STATUS` | 判定結果を保持するテーブル | 履歴表示のたびに30日分を集計すると重い。日次バッチで事前計算する |
| `POS_SALE_EVENT` | `(client_id, event_id)` に一意制約 | 販売イベントの再送で在庫を二重に減算しないため |
| `INVENTORY_SNAPSHOT` | `(sku_id, snapshot_date)` に一意制約 | バッチの二重実行で同日を重複記録しないため |
| `RESTOCK_SUBSCRIPTION` | 会員×SKUで有効な登録は1件 | 部分一意インデックス（`state = 'ACTIVE'` の条件付き）で担保 |
| 金額 | 整数（円） | 浮動小数点による誤差を避ける |
| 日時 | UTC で保存し、日本時間で表示 | 日付境界の判定を一貫させる |

---

## 4. API一覧

BFF は `/api/*` を受け、Backend の同名パスへ転送する。以下は Backend 側の契約を示す。

### 4.1 エンドポイント一覧

| # | メソッド・パス | 認可 | 概要 |
|---|---|---|---|
| 1 | POST /auth/login | 公開 | ログインし、トークンを Cookie に設定する |
| 2 | POST /auth/refresh | リフレッシュトークン | アクセストークンを再発行する |
| 3 | POST /auth/logout | 認証済み | トークンを失効させる |
| 4 | GET /me/orders | 本人のみ | 自分の購入履歴と販売状態を取得する |
| 5 | GET /skus/{sku_id}/sales-status | 公開 | SKUの販売状態を取得する |
| 6 | POST /me/orders | 本人のみ | 履歴から同一SKUで再購入する |
| 7 | POST /me/restock-subscriptions | 本人のみ | 再入荷通知に登録する |
| 8 | DELETE /me/restock-subscriptions/{id} | 本人のみ | 通知登録を解除する |
| 9 | GET /me/notifications | 本人のみ | 自分の通知を取得する |
| 10 | POST /pos/sales | POS専用資格情報 | 販売イベントを受け付ける |

**パスに会員IDを含めない。** 対象の会員はアクセストークンの `sub` からのみ決定する。
これにより、URLのIDを差し替えて他人の情報を要求する経路そのものを作らない（S-02）。

### 4.2 入出力の型定義

型は TypeScript 表記で示す。Backend 側は Pydantic モデルで同じ制約を定義する。

#### 4 GET /me/orders

入力（クエリ文字列）

| キー | 型 | 必須 | 既定 | 制約 |
|---|---|---|---|---|
| page | integer | 任意 | 1 | 1以上1000以下 |
| per_page | integer | 任意 | 20 | 1以上100以下 |

出力

```typescript
type SalesStatusCode = "ON_SALE" | "OUT_OF_STOCK" | "DISCONTINUED" | "UNKNOWN";

type OrderHistoryItem = {
  order_line_id: string;              // UUID
  ordered_at: string;                 // ISO 8601
  product_code: string;
  product_name: string;
  color: string;
  size: string;
  quantity: number;                   // integer
  purchased_unit_price_yen: number;   // integer 購入時の単価
  current_unit_price_yen: number | null; // 終売時は null
  sales_status: SalesStatusCode;
  evaluated_at: string | null;        // 判定日時 ISO 8601
  can_reorder: boolean;
  can_subscribe: boolean;
};

type OrderHistoryResponse = {
  items: OrderHistoryItem[];
  page: number;
  per_page: number;
  total: number;
};
```

`can_reorder` と `can_subscribe` をサーバが返すのは、表示可否の判断をクライアントに委ねない
ためである。クライアントはこの値に従うが、サーバは操作時に改めて検証する。

#### 6 POST /me/orders

入力

```typescript
type CreateOrderRequest = {
  sku_id: string;                     // UUID
  quantity: number;                   // integer 1以上10以下
  expected_unit_price_yen: number;    // integer 画面に表示していた単価
};
```

出力

```typescript
type CreateOrderResponse = {
  order_id: string;                   // UUID
  total_yen: number;
  ordered_at: string;
};
```

#### 5 GET /skus/{sku_id}/sales-status

出力

```typescript
type SalesStatusResponse = {
  sku_id: string;
  sales_status: SalesStatusCode;
  evaluated_at: string | null;
};
```

在庫数と連続在庫0日数は返さない。いずれも在庫量や仕入の状況を推測させる内部指標であり、
顧客の判断に必要な情報は販売状態の区分だけで足りるためである。連続在庫0日数と判定理由は
`SALES_STATUS` に保持するが、運用・調査のための内部データとして扱い、顧客向けAPIでは返さない。

#### 7 POST /me/restock-subscriptions

```typescript
type CreateSubscriptionRequest = { sku_id: string };
type CreateSubscriptionResponse = { subscription_id: string; state: "ACTIVE" };
```

#### 10 POST /pos/sales

入力

```typescript
type PosSaleRequest = {
  event_id: string;      // POS側で一意。同一取引明細に対して同じ値
  store_id: string;      // UUID
  sku_id: string;        // UUID
  quantity: number;      // integer 1以上100以下
  occurred_at: string;   // ISO 8601
};
```

出力

```typescript
type PosSaleResponse = {
  accepted: boolean;
  duplicated: boolean;   // 既に処理済みのイベントなら true
};
```

---

## 5. 入力値の下限・上限

| 対象 | 型 | 下限 | 上限 | 既定 | 超過時 |
|---|---|---|---|---|---|
| page | integer | 1 | 1000 | 1 | 422 |
| per_page | integer | 1 | 100 | 20 | 422 |
| quantity（再購入） | integer | 1 | 10 | 1 | 422 |
| quantity（POS販売） | integer | 1 | 100 | — | 422 |
| email | string | 1文字 | 254文字 | — | 422 |
| password | string | 12文字 | 128文字 | — | 422 |
| expected_unit_price_yen | integer | 0 | 1,000,000 | — | 422 |
| occurred_at | ISO 8601 | 現在時刻−7日 | 現在時刻＋5分 | — | 422 |
| 履歴の遡及年数（設定値） | integer | 1 | 5 | 3 | 起動時エラー |
| 終売判定日数（設定値） | integer | 1 | 365 | 30 | 起動時エラー |
| スナップショット保持日数（設定値） | integer | 30 | 1825 | 1095 | 起動時エラー |

`occurred_at` に範囲を設けるのは、POS側の時計ずれや過去日時の送信によって在庫の整合性が
崩れることを防ぐためである。範囲外は受理せず、運用側で確認する。

設定値は起動時に検証し、範囲外なら起動を失敗させる。実行中に不正な閾値で判定が走ることを
防ぐ。

---

## 6. エラー処理

### 6.1 HTTPステータスの割り当て

| ステータス | 条件 | 画面表示 | ログに残すもの／残さないもの |
|---|---|---|---|
| 400 | 形式不正（JSONが壊れている等） | 入力内容をご確認ください | 残す：パス、エラー種別／残さない：本文全体 |
| 401 | 未認証、トークン失効 | ログインしてください | 残す：パス／残さない：トークン |
| 403 | POS資格情報が対象店舗の権限を持たない | — | 残す：client_id、store_id |
| 404 | 商品が存在しない。**他人の注文・通知登録を指定した場合も404** | 対象が見つかりません | 残す：パス、認証済み会員ID／残さない：要求されたID |
| 409 | 在庫不足、価格変更、状態の不整合 | 在庫がありません／価格が変更されています | 残す：sku_id、判定結果 |
| 422 | 型・範囲の検証エラー | 項目ごとの説明を表示 | 残す：項目名と違反種別／残さない：値そのもの |
| 429 | 試行回数の超過（ログイン等） | しばらくしてからお試しください | 残す：発生元 |
| 500 | 想定外の例外 | 時間をおいてお試しください | 残す：スタックトレース、相関ID |

### 6.2 他人のデータに404を返す理由

権限のないリソースに403を返すと、「そのIDのリソースが存在すること」を要求者に伝えてしまう。
存在の有無自体を秘匿するため、他人の注文・通知登録に対しては存在しない場合と同じ404を返す。

なお本設計ではパスに会員IDを含めない構造（4.1）を採っているため、この経路は限定的である。
`DELETE /me/restock-subscriptions/{id}` のように識別子をパスに含めるものが対象になる。

### 6.3 バッチ・連携の失敗

| 事象 | 処理 |
|---|---|
| 日次バッチが途中で失敗 | 当日分を再実行できる。一意制約と再計算方式により二重記録・過大計上が起きない |
| バッチが当日実行されなかった | 翌日の実行で欠測日が生じる。欠測日は在庫0とみなさず、判定不可の扱いとする |
| 販売イベントの重複受信 | `(client_id, event_id)` の一意制約で検知し、在庫を変更せず受理済みを返す |
| 在庫が不足する販売イベント | 在庫を0で止め、差異を記録する。在庫を負にしない |
| 模擬決済の失敗 | 注文を成立させず、在庫の減算も行わない |

---

## 7. 状態遷移

### 7.1 販売状態

```mermaid
stateDiagram-v2
    [*] --> UNKNOWN : SKU登録直後
    UNKNOWN --> ON_SALE : スナップショットが必要日数そろい在庫あり
    UNKNOWN --> OUT_OF_STOCK : スナップショットがそろい在庫なし
    ON_SALE --> OUT_OF_STOCK : 在庫が0になる
    OUT_OF_STOCK --> ON_SALE : 在庫が1以上になる
    OUT_OF_STOCK --> DISCONTINUED : 連続在庫0日数が30日に達する
    ON_SALE --> DISCONTINUED : SKUまたは品番が無効化される
    OUT_OF_STOCK --> DISCONTINUED : SKUまたは品番が無効化される
    DISCONTINUED --> ON_SALE : 在庫が復活、または再度有効化される
```

`DISCONTINUED` は最終状態ではない。誤判定や再販に対応するため、在庫が復活した場合は
`ON_SALE` へ戻し、連続在庫0日数を0にリセットする。

### 7.2 再入荷通知登録

```mermaid
stateDiagram-v2
    [*] --> ACTIVE : 品切れ状態のSKUに登録
    ACTIVE --> NOTIFIED : 在庫が0から1以上へ変化し通知を作成
    ACTIVE --> CANCELLED : 購入者が解除
    NOTIFIED --> [*]
    CANCELLED --> [*]
```

通知は1登録につき1回。再度待ちたい場合は登録し直す。終売と判定されたSKUには新規登録を
受け付けない。

---

## 8. セキュリティ設計

### 8.1 認証（JWT）

| 項目 | 設計 |
|---|---|
| アクセストークン | 有効期限15分。署名アルゴリズムを明示的に固定し、`alg: none` や算法差し替えを受け付けない |
| リフレッシュトークン | 有効期限7日。データベースに保管し、使用時にローテーションする。再利用を検知したら系列全体を失効させる |
| 保管場所 | HttpOnly・Secure・SameSite=Lax の Cookie。**localStorage や sessionStorage に置かない** |
| 失効 | ログアウト時にリフレッシュトークンを削除する |
| パスワード | bcrypt 等のハッシュ関数で保存する。平文・可逆暗号で保存しない |

localStorage を使わないのは、スクリプト経由でトークンが読み出される事故を、保管方法の段階で
防ぐためである。HttpOnly Cookie はJavaScriptから参照できない。

### 8.2 認可（本機能の最重要項目）

本機能の中核データは購入履歴であり、これは個人の購買行動そのものである。他人に見られては
ならない。

| 対策 | 内容 |
|---|---|
| 会員の特定 | アクセストークンの `sub` からのみ会員を決定する。リクエストボディ・クエリ・パスの会員IDを一切信用しない |
| 経路の遮断 | `/me/...` の構造とし、パスに会員IDを持つエンドポイントを作らない |
| 所有者検証 | 識別子をパスに含めるもの（通知登録の解除など）は、必ず `resource.member_id == token.sub` を検証する |
| 検証位置 | 検証は Backend で行う。BFF や画面側の表示制御だけに依存しない |
| 情報漏れの抑止 | 他人のリソースには404を返し、存在の有無を伝えない（6.2） |

### 8.3 BFF（リバースプロキシ）

- ブラウザは BFF とのみ通信する。Backend API はインターネットに公開しない。
- BFF は Cookie からトークンを取り出し、Backend への `Authorization` ヘッダに詰め替える。
  ブラウザ側のJavaScriptはトークンを保持しない。
- BFF は転送先パスを固定の許可リストで制限し、任意のURLへ中継しない（SSRF対策）。
- Backend は BFF の送信元のみを受け付けるようネットワークで制限する。

### 8.4 CORS

- ブラウザから見て BFF は同一オリジンであるため、通常の画面操作でCORSは発生しない。
- Backend 側の CORS 設定は、許可オリジンを BFF のオリジンのみに限定する。`*` を使わない。
- 認証情報を伴う通信では、仕様上 `allow_origins` にワイルドカードを指定できない。
  許可オリジンは環境変数で明示する。
- 許可メソッド・許可ヘッダも必要なものだけに限定する。

### 8.5 サーバ側での計算と照合

| 対象 | 設計 |
|---|---|
| 金額 | クライアントから受け取った `expected_unit_price_yen` を保存や請求に使わない。単価はDBから取得して再計算し、一致しない場合は409で拒否する |
| 販売状態 | クライアントが送る `can_reorder` 等を信用しない。注文処理時に改めて判定する |
| 在庫 | 表示時の値ではなく、注文確定時に行ロックを取って再判定する |
| 数量 | 上限10をサーバ側で検証する。画面の入力制限だけに依存しない |

画面上の制限を回避したリクエストでも、不正な注文が成立しないことを確認する。

### 8.6 API仕様書の非公開

FastAPI は既定で `/docs`、`/redoc`、`/openapi.json` を公開する。本番環境ではすべて無効化
する。`openapi.json` を残すとスキーマ全体が取得できるため、3つとも止める。

```python
is_production = settings.ENV == "production"

app = FastAPI(
    docs_url=None if is_production else "/docs",
    redoc_url=None if is_production else "/redoc",
    openapi_url=None if is_production else "/openapi.json",
)
```

### 8.7 SQLインジェクション対策

| 層 | 対策 |
|---|---|
| Frontend | TypeScript で API の入出力型を定義し、想定外の値の送信をコンパイル時に検出する |
| Backend 入口 | Pydantic モデルで型・範囲・書式を検証する。UUIDはUUID型として受け取り、文字列のまま扱わない |
| データアクセス | SQLAlchemy のクエリビルダを使い、値をパラメータとしてバインドする |
| 生SQL | 集計など必要な箇所では `text()` とバインドパラメータを用いる。文字列連結・書式化でSQLを組み立てない |

```python
# 不可：値をSQL文字列に埋め込む
session.execute(text(f"SELECT * FROM sku WHERE barcode = '{barcode}'"))

# 可：パラメータとしてバインドする
session.execute(
    text("SELECT * FROM sku WHERE barcode = :barcode"),
    {"barcode": barcode},
)
```

### 8.8 依存ライブラリの脆弱性

- 採用するフレームワーク・ライブラリのバージョンを一覧化し、実装開始時にサポート状況と
  既知の脆弱性を確認して固定する。
- 依存関係の検査（Python は `pip-audit`、Node.js は `npm audit`）を実施し、継続的に
  確認できるようにする。
- ロックファイルをリポジトリに含め、環境間でバージョンを一致させる。
- 検査結果と対応方針を記録し、対応できないものは理由を残す。

### 8.9 秘密情報とログ

- 認証情報・署名鍵・接続情報は環境変数で管理し、リポジトリに含めない。
- ログにトークン、パスワード、購入履歴の内容を出力しない。
- エラーには相関IDを付与し、利用者には内部構造を表示しない。
- 公開環境ではHTTPSを使用する。

---

## 9. 検証計画

要件定義書の受け入れ確認項目 A-01〜A-14 を受け入れテストとして実装する。特に重要なものを
以下に示す。

### 9.1 境界値

| 対象 | 確認内容 |
|---|---|
| 終売判定 | 連続在庫0日数が29日では「品切れ」、30日で「終売」になる |
| 遡及期間 | 3年ちょうど前の購入が表示され、3年と1日前は表示されない |
| 判定不可 | スナップショットが29日分では「判定不可」、30日分そろうと判定される |
| 数量 | 10点は成立し、11点は422で拒否される |
| ページング | per_page=100は成立し、101は422で拒否される |

### 9.2 認可

| 対象 | 確認内容 |
|---|---|
| 他人の通知登録 | 他人の登録IDを指定した解除が404になり、実際に解除されない |
| 未認証 | ログインせずに `/me/orders` を要求すると401になる |
| トークン改ざん | 署名を書き換えたトークンが拒否される |
| POS資格情報 | 購入者の認証情報で `/pos/sales` を呼べない |

### 9.3 同時実行・冪等性

| 対象 | 確認内容 |
|---|---|
| 同時注文 | 在庫1のSKUへ同時に2件注文すると、成立は1件のみ。在庫が負にならない |
| イベント再送 | 同一 `event_id` を2回送信しても在庫の減算は1回 |
| バッチ二重実行 | 同日に2回実行しても、スナップショットが重複せず連続在庫0日数も変わらない |
| バッチ再実行 | 途中失敗後の再実行で、判定結果が単独実行時と一致する |

### 9.4 検証用データ

在庫スナップショットを含む初期データを用意する。

- SKU：在庫あり／在庫0が29日継続／在庫0が30日継続／在庫0が31日継続／SKU無効化済み／
  スナップショットが29日分しかないもの、を各1件以上。
- 購入履歴：3年以内と3年より前の購入、価格が改定された商品、終売品を含む。
- 店舗：複数店舗にまたがる在庫を持つSKUを含める。

### 9.5 検証環境の注意

同時実行の検証は PostgreSQL 上で、独立した接続・トランザクションを用いて実際に並行実行
する。SQLite では行ロックの挙動が異なるため、在庫の排他制御の検証には使用しない。

---

## 10. 既存POSへの改修

Lv2で開発したPOSは単一店舗で会計を完結させる前提で成立していた。ECと接続するにあたり、
以下の改修を行う。

| # | 改修内容 | 影響範囲 |
|---|---|---|
| 1 | 商品管理を品番単位からSKU単位へ変更する | 商品マスタ、バーコード読み取り |
| 2 | 取引明細にSKUを保存する | 取引明細テーブル、会計確定処理 |
| 3 | 取引に会員IDを保存する | 取引テーブル、会員ID読み取り処理 |
| 4 | 取引に店舗IDを保存する | 取引テーブル、設定 |
| 5 | 会計確定時に店舗×SKUの在庫を減算する | 会計確定処理、在庫テーブル（新規） |
| 6 | 商品マスタの終売を論理削除に変更する | 商品マスタ、参照箇所 |
| 7 | 販売イベントをEC側へ送信する | 会計確定処理、連携クライアント（新規） |

7の送信は会計確定と同一トランザクションで確定させず、送信用のテーブルに記録したうえで
別処理が送る方式とする。EC側との通信失敗が店頭の会計を止めないためである。`event_id` は
取引ID・明細IDから決定的に生成し、再送しても同じ値になるようにする。

---

## 11. 未決定事項

| ID | 未決定事項 | 現時点の案 |
|---|---|---|
| D-01 | 採用バージョン | 実装開始時にサポート状況と脆弱性を確認して固定する |
| D-02 | バッチの実行基盤と時刻 | 店舗の閉店後に日次実行する案。基盤は未定 |
| D-03 | 欠測日の扱い | バッチ未実行日は在庫0とみなさず判定不可とする案（6.3） |
| D-04 | 終売判定の対象範囲 | 全SKUを対象とする案。負荷が問題になる場合は履歴に存在するSKUに絞る |
| D-05 | 通知の配信方法 | アプリ内通知のみとする案 |
| D-06 | 公開先・データ保持期間 | 公開前に具体化する |
| D-07 | 補充サイクルの実測 | 終売判定の30日は週次補充という仮定に基づく。実績で見直す |

D-03とD-07は判定結果の正しさに直接関わるため、実装前に優先して確定する。

---

## 12. 参照

- スクール配布資料『Lv3のスコープを決める』：課題の立て方、対象外の明示、将来スコープ、
  数値とその理由、POS変更の検討観点。
- GU公式が公開する在庫表示ルール：在庫表示に閾値が設けられていること、および画面上の在庫と
  実在庫にズレが生じることの参考。
- [FastAPI 公式ドキュメント：Metadata and Docs URLs](https://fastapi.tiangolo.com/tutorial/metadata/)
- [MDN：Cross-Origin Resource Sharing (CORS)](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS)
- [MDN：Set-Cookie（HttpOnly、Secure、SameSite）](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie)
- [SQLAlchemy 公式ドキュメント：Textual SQL とバインドパラメータ](https://docs.sqlalchemy.org/en/20/core/sqlelement.html#sqlalchemy.sql.expression.text)

上記は仕組みを確認するための参照であり、特定バージョンの採用を確定するものではない。
本書における設計意図の解釈には推論が含まれる。

---

## 13. 生成AIの利用

<!-- 本人が記入 -->
